const list=document.querySelector('#series-list');const dialog=document.querySelector('#lightbox');const dimg=dialog.querySelector('img');const dtitle=dialog.querySelector('strong');const dmeta=dialog.querySelector('span');fetch('content/series.json').then(r=>r.json()).then(series=>{series.forEach(s=>{const row=document.createElement('section');const visibleWorks=s.works.filter(w=>w.id!=='market-03');row.innerHTML=`<div class="series-row"><span class="number">${s.number}</span><h3>${s.title}</h3><p>${s.description}</p><span class="year">${s.year}<br>＋</span></div><div class="series-content"><div class="gallery">${visibleWorks.map(w=>`<article class="work"><button data-img="${w.image}" data-title="${w.title}" data-meta="${[w.year,w.materials,w.size].filter(Boolean).join(' · ')}"><img src="${w.image}" alt="${w.title}" loading="lazy"></button><div class="caption"><span>${w.title}</span><span>${[w.year,w.materials,w.size].filter(Boolean).join(' · ')}</span></div></article>`).join('')}</div></div>`;list.appendChild(row);row.querySelectorAll('.work img').forEach(img=>{const setOrientation=()=>img.closest('.work').classList.add(img.naturalWidth>img.naturalHeight?'landscape':'portrait');img.complete?setOrientation():img.addEventListener('load',setOrientation,{once:true})});row.querySelector('.series-row').addEventListener('click',()=>{row.querySelector('.series-content').classList.toggle('open');row.querySelector('.year').innerHTML=row.querySelector('.series-content').classList.contains('open')?`${s.year}<br>−`:`${s.year}<br>＋`});});document.querySelectorAll('[data-img]').forEach(b=>b.addEventListener('click',()=>{dimg.src=b.dataset.img;dtitle.textContent=b.dataset.title;dmeta.textContent=b.dataset.meta;dialog.showModal()}));});dialog.querySelector('button').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});const mb=document.querySelector('.menu-button');const nav=document.querySelector('.top nav');mb.addEventListener('click',()=>{nav.classList.toggle('open');mb.setAttribute('aria-expanded',nav.classList.contains('open'))});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));